---
name: stories-resource-tables
description: Story-library resource-table and prompt conventions — frame terminology (face/side/edge/view/word), image prompt ordering and segmentation rules, image and MiniMax H3 video size buckets (entity modeling picks portrait/landscape/square by the object's own shape), the H3 prompt language rules (English body, verbatim skeleton), the measured cost of Chinese bodies, entity/scene-still resource types (single-view modeling, grayscale mask, variation, Qwen-Image-Edit wording, nine-grid, scene first-frame/pull-in/push-in/orbit), table md field types, and the overhead view. Use when writing or editing any image-type resource table, or when a task needs the shared size, segmentation, or H3 language rules.
---

# 故事库资源表与提示词通用规范（总纲）

> 本技能是资源表的**总纲**：尺寸规范、分段规则与 H3 语言规范对**所有**资源类型都适用，场景与视频类技能会引用它。各类型条目分散在技能 `stories-scene-video`、`stories-video-prompts`、`stories-audio-resources` 中。

## 画面术语定义（面/侧/边/视/词）

五种术语全文档严格区分，严禁混用：

1. **面**：代表三维空间拍摄的具体画面（场景某一朝向的画面），共**九方位**：前面（正面）、前右、右面、右后、后面、后左、左面、左前、全图（四个正方向 + 四个斜方向 + 全景俯瞰）；场景类列名与提示词内容中的「面」一律指被描述的画面朝向
2. **侧**：代表摄像机在哪侧，**永远与面相对**：左侧相机拍摄的是右面，右侧相机拍摄的是左面，后侧相机拍摄的是前面，前侧相机拍摄的是后面；斜向同理——左前侧相机拍摄的是右后面，右后侧相机拍摄的是左前面，后左侧相机拍摄的是前右面，前右侧相机拍摄的是后左面；上下同理（上侧相机拍摄的是下面，下侧相机拍摄的是上面）
3. **边**：代表一个场景画面九宫格的哪一边（方位格），例如 左边在中间的左边，右上在中间的右边的上边
4. **视**：与场景无关，只与万物有关；万物类画面**只画单一视角**（按对象形态取最能体现特征的一面：人物/生物取正面，物品取最能看清本体的一面），**不再并排展示正视/侧视/背视**
5. **词**：仅分 正词 与 负词；资源表提示词列后缀一律写 -正词 / -负词，词名中不得出现「面」字（避免与画面的面冲突）；提示词正文中出现的「面」指画面的面（如旋镜提示词「右面是…」）

## 资源表

所有资源文件均为表格 md 文件，全文件仅由一个表格构成，严禁出现任何标题

### 图片提示词书写顺序规范

万物建模、万物变化、场景首帧、场景拉镜、场景推镜 的提示词，除遵循各自类型的专属要求外，还必须严格按照以下顺序书写：

1. **整体图片品质**：描述整图画质、清晰度、精细度、光影氛围、环境背景等整体视觉表现
2. **图片元素万物**：列出画面中出现的所有元素与万物（人物、器物、场景物体、生物、道具、自然景物等）
3. **图片风格要求**：写明整体艺术风格（如CG动漫、水墨国风、写实等）。**本故事库所有生图类资源（万物建模、万物变化、场景首帧/拉镜/推镜/旋镜）默认统一采用「国漫CG风格」，严禁出现"写实"/"写实CG"字样**

三段顺序不可颠倒，缺失任一段视为不合格。

#### 提示词内容分段规则（通用，两类生图提示词共同遵守）

- **段分隔符统一**：提示词的所有分段点一律用 `<br>` 分隔（每个分段点一个 `<br>`，置于段末），段内属性用全角逗号 `，` 分隔，全文不使用句号；严禁使用换行符（真实回车 / 换行字符）或 `
`（反斜杠 n 两字符）作为分段符——md 表格单元格内的换行会被解析为新行从而断开表格，只有 `<br>` 能在单元格内安全分段；冒号仅用于点名词句（如万物类的 `主体：`），同一条目内要么全用要么全不用，保持一致
- **一段一职责**：每段只写一类信息（整图品质 / 一个显示区域 / 一个方位格 / 风格），不跨段混写
- **段头点名词**：每段以点名词开头（如 `主体：`、`左上…`、`右面是`、`整体是…`、`国漫CG风格`），点名词即段边界
- **一个元素只归一段**：不得在不同段重复堆叠同一元素
- **风格词只在最后一段**出现（`国漫CG风格，…` 收尾），严禁散入品质段与区域段
- **`整体是…` 总结段全文仅一处**，紧跟区域段之后
- **段序固定**，不可颠倒，缺段视为不合格

两类提示词的分段维度不同，各自按以下规则执行：

#### 万物类分段规范（按单一完整画面分段）

万物建模、万物变化 的画面是**单一完整画面**（只画一个视角的完整外观），**不使用三视图、不划分特写分区、不使用九宫格**。段序：

1. **品质段**：画质描述 + 纯白背景硬句（#FFFFFF，无环境、阴影、场景，见各自类型规范）
2. **布局声明段**：点明画面构成与取景（`正面全身立绘，人物居中构图，从头到脚完整入画，画面只呈现这一个正面形象` / `物品完整外观居中展示，画面只呈现这一个物品形象`）
3. **主体段**：`主体：` 开头，逐条写该视角下可见的形态特征（外形轮廓、结构组成、材质质感、颜色配色、纹样装饰、配件部件）
4. **总结段**：`整体是…`（全文仅一次）
5. **风格段**：`国漫CG风格，…` 收尾

点名词体系：**主体**（单一视角的画面主体）。`主体：` 只与万物有关，**严禁出现在场景类提示词中**；万物类**严禁出现三视字样与特写分区**（`三视图`、`全身三视图`、`物品三视图`、`正视/侧视/背视`、`左边特写`）。
万物变化段序：`以图1为底，保持原构图不变` 起头 → 主体修改段（`主体：` 开头，写改后的新外观）→ 风格段。

#### 场景类分段规范（按九宫格方位格分段）

场景首帧、场景拉镜、场景推镜、场景旋镜 的画面按**九宫格方位格**分段（见「场景图片生成 九宫格画面描述要求」）。段序：

1. **品质段**：画质 + 光影氛围描述（场景自带环境，无纯白背景要求）
2. **方位格段**：按固定顺序逐格一段——16:9 横版按 `左上…` → `左边…` → `左下…` → `上边…` → `中间…` → `下边…` → `右上…` → `右边…` → `右下…`；9:16 竖版按 `上左…` → `上边…` → `上右…` → `左边…` → `中间…` → `右边…` → `下左…` → `下边…` → `下右…`；每格段以该格方位词（边词）开头，无内容的格可略过
3. **风格段**：`国漫CG风格，…` 收尾

点名词体系：**边词**（九宫格方位格），与万物类的 `主体：` 点名词互斥——场景类提示词中**严禁出现万物类的 `主体：` 点名词、三视字样与特写分区**（`正视/侧视/背视`、`三视图`、`左边特写`）。
特例：

- **场景旋镜**：内容段前先写固定前缀（`图1 是正面参考图<br>当前在生成x面，画面描述如下：<br>`，x面 ∈ 正面/前右/右面/右后/后面/后左/左面/左前，见「场景旋镜 提示词提取规范」）；内容段以**面词**点面（`前右是`/`右面是`/`右后是`/`后面是`/`后左是`/`左面是`/`左前是`），段内按九宫格顺序逐格写并对照 图1(前面) 保持空间一致，以 `整体是…` 收尾；**全图**俯瞰画面不适用本旋镜前缀模板，按「全图方位」要求另写俯视平面图描述
- **场景推镜**：单帧非九宫格，不划分方位格段，仅用区域方位词描述画面内各区域的内容分布

### 生图类尺寸规范

**万物建模、万物变化按对象形态选横竖屏**——画面只画单一完整画面，比例必须贴合对象固有形态，否则主体四周会留大片空白：

| 对象形态 | 比例 | 宽度×高度 |
| :--- | :--- | :--- |
| 站立/竖高的对象（人物、生物、立式器物、高塔等） | 9:16 竖屏 | **768×1344** |
| 横向铺展的对象（桌面器物、横放的书籍、车辆等） | 16:9 横屏 | **1344×768** |
| 无明显横竖倾向的对象（正方、圆形、球状等） | 1:1 正方 | **768×768** |

- 判定看**对象本体的固有形态**，不看画面里想放多少元素；拿不准时归入最接近的一档
- 宽度/高度两列必须成对填写上表中的整组数值，不得自行拼凑比例
- 显存紧张时：竖屏降 480×832、横屏降 832×480（0.4MP）；高清统一走 4x 外部放大 + 分块重绘
- 像素保持 16 的倍数（VAE 8×8 下采样对齐）；三档比例与 H3 视频合法桶一致，下游首帧视频可直接等比缩放不裁切

**场景首帧**（场景类，不属于万物）的宽度/高度统一填写 **16:9 = 1344×768**（高清源图，8G 显卡可跑 Qwen 4 步 Lightning 档）。进首帧视频/首帧场景时，模型会把首帧自动重采样到视频目标 canvas（如 832×480），无需人为改小；同 16:9 比例下直接等比缩放不裁切。需要其他比例时，从 H3 合法桶选取（见「视频类尺寸规范」）。

### 视频类尺寸规范（MiniMax H3）

视频类资源（文生场景、首帧场景、文生视频、首帧视频、首尾视频、关键帧视频、参考视频）的宽度/高度按 MiniMax H3 官方 canvas 规则填写：

- H3 固定 **24fps**（模型架构常量：表内**不设「视频帧率」列**，工作流的帧数换算一律写死 ×24，输出容器帧率亦恒为 24；要变速/补帧另用 `FallingTSFrameRateConvert` 节点）；视频秒数按需求填 5~15 秒（官方训练域）
- canvas 硬规则：**短边 ≤ 768**、**面积 ≤ 768×1344（≈1.03MP）**、每轴为 **32 的倍数**；宽度/高度直接决定生成尺寸，超限不会被自动纠正
- 默认统一 16:9 = **832×480**（480p 低显存档，0.4MP，官方原生工作流默认，适配 8G 显卡）
- 显存充足（16G+）可升档 16:9 = **1344×768**（768p 官方主推档，与 turbo 4 步 LoRA 训练域一致）
- 其余比例合法桶：21:9 = 1536×672、3:2 = 1152×768、4:3 = 1024×768、1:1 = 768×768、3:4 = 768×1024、2:3 = 768×1152、9:16 = 768×1344、9:21 = 672×1536
- 低显存/加速档 9:16 = **480×832**
- **严禁使用 1664×928 等生图桶值**：面积 1.54MP 超出 H3 上限约 50%，属分布外，易质量崩坏；2K 需另行二采放大（SeedVR2 / RTX VSR），不在宽度/高度列表达

### H3 提示词语言规范（视频类资源通用）

**写进资源表的提示词正文一律英文**，只有对白/歌词与画面内实际可见的文字保留原语言。

依据：H3 官方提示词技能（`h3-prompt-writing`）与官方模板（`templates\视频\api_minimax_h3_max_r2v.json` 等）的示例 prompt 全为英文；官方 HF 仓库讨论 #32 中经官方人员编辑的答复为「除需要生成的文字和对白内容，其他都要用英文描述」；官方模型卡载明 H3-Encoder 取 **Qwen3-VL-32B 第 50 层隐状态**送进 H3-Omni-Transformer，生成主干是在英文条件上训练的，故中文条件落在分布外。本机 `qwen3vl_32b_minimax_h3_nvfp4_awq` 是 **4-bit** 量化编码器，多语言基准实测长提示词的词义错乱率为**英文 1.8 / 100 词、中文 3.1 / 100 词**（中文额外损失约 1.7 倍）——长提示词用英文可同时规避「条件分布外」与「量化损失」两笔代价。

| 内容 | 填写语言 |
| :--- | :--- |
| 六部分 / 三部分核心字段的正文（画面、运镜、光影、动作、声音描述） | **英文** |
| 固定字段名与标签 | **英文**：`subject_definitions`、`integrated_multimodal_description`、`<Subject N>`、`<Picture N>`、`<Video N>`、`<Audio N>`、`[Shot N]`、`At MM:SS.mmm`、`fully_preserved` 等；**严禁写成 `<图片 1>`、`[镜头 1]`、`[中文]`** |
| 对白语言标签 | **英文语言名**：`<d>[Chinese] 我们现在出发。</d>`；说话人 ID 写 `(S1)`、`(S2)` 并置于 `<d>` 之外 |
| `<d>` 内的对白 / 歌词 | **保留原语言**（中文对白直接写中文，不翻译、不改写） |
| 画面内实际可见的文字（招牌 / 字幕 / 标签） | **保留原语言**，用英文双引号逐字包住，如 `a sign reading "营业中"` |
| 镜头语言术语（见下文词汇表） | 写入提示词时取**英文术语**（push in / medium close-up / cross-dissolve），词汇表中的中文词仅供对照 |

**骨架必须逐字照抄（译错即失效）**：`<d>` 与 `</d>` 不是普通文字，而是 tokenizer 里的**扩展 special token**（本机 `ComfyUI\comfy\text_encoders\minimax.py` 中 id 为 **151669 / 151670**；同批还有 `<|cutoff|>`=151671、`<|lyrics_start|>`=151672、`<|lyrics_end|>`=151673、`<|caption_start|>`=151674、`<|caption_end|>`=151675，全部在基础词表 0..151668 之外）。译成 `<对话>` 只会被切成两个普通 token，指令直接失效；整枚标签**连同数字与空格一并照抄**（`<Picture 1>`，不是 `<Picture1>`、不是 `<图片 1>`）。字段名（`subject_definitions:` 等）、retention 枚举（`fully_preserved` 等）、任务前缀（`[reference generation]`）、`[Shot N]`、时间戳、`(S1)` 同理，全部属于骨架。

- **`<d>` 内只放两样东西**：英文语言名标签 + **用户原始说话内容**（官方 `base-en.txt`：`Inside <d>, include only the language tag and the actual user-provided spoken content`）。说话人身份、音色、语气、语速、动作一律写在 `<d>` **之外**；`<d>` 内的对白逐字保留、不翻译不改写。
- **`<d>` 内标点**：只用普通的 `,` `.` `?` `!`，且在 `</d>` 之前收尾；不写全角标点、不写省略号。
- **引用参考音频的原词**：保留原语言逐字；听不清的片段写 `[unclear]`，不得猜写。

**本文档各节的规范说明与示例用中文书写是为便于阅读；填进资源表的提示词正文一律英文。** 各类对齐指令的官方英文原句如下（下列中文句仅作对照，实际填写用英文）：

- I2VA 首行：`For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced.`
- FL2VA 首行：`Reference image alignment with the target video - Picture 1 (from [Shot 1]) aligns with the 0.00-second position of the target video; Picture 2 (from [Shot N]) aligns with the S.SS-second position of the target video.`
- 关键帧锚定：`[Shot N] At S.SS, anchored to the frame established by <Picture N>.`

**唯一的例外是短提示词**：官方多语言基准显示短提示词在 13 种语言下均无量化损失，日常氛围描述用中文不掉质量；但本库的视频 / 场景提示词普遍为数百至上千词的长结构化提示词，正落在损失区，故一律按英文写。

#### 中文正文的实测代价（2026-09-18 本机受控实验）

上条"一律英文"不是照搬教条，本机用 **11 组同 seed 生成**把它量过一遍（0044 参考视频三镜 8/11/14s、1344×768、24fps、345 帧；指标用 `scripts\face-sim.py` 的 ArcFace 余弦 + genderage 年龄估计，取尾镜 4~5 帧均值）。结论：**中文正文能正常出片、六大字段与时间轴结构无损，但有稳定且可复现的代价**。

| 指标 | 英文（4 次） | 中文（7 次） | 判定 |
| :--- | :--- | :--- | :--- |
| **人脸年龄感** | 25.2 / 26.1 / 25.8 / 25.6 | 27.8 / 27.4 / 28.7 / 29.4 / 29.4 / 27.4 / 27.1 | **零重叠**：中文稳定把同一张参考脸"拍熟"约 **2~3 岁**，跨 2 个 seed 方向一致，是本轮唯一干净分层的差异 |
| **取景（脸高/画面）** | 0.29 / 0.35 / 0.35 / 0.75 | 0.39~0.84（5 次 ≥ 0.54） | **方差大，不能算语言效应**（同语言不同 seed 可差 0.79 vs 0.39）；但把身份段回英文能显著收近（见 C6） |
| **几何相似度** | 0.518~0.713 | 0.556~0.677 | **无稳定语言效应**：中文并不更低（中文基线 0.677 > 英文基线 0.609） |

**必须撤回的旧判断**：早期据单帧目视得出的"中文版人脸身份明显变弱、像换了个人"**是错的**。那是**景别混淆**——中文版尾镜恰好把脸拍满画面（脸高 0.79 对英文 0.35），放大后的纹理被误读成"五官变了"。等脸高拼图（`scripts\H3身份补足-等脸高对照.png`）显示 11 个版本都是同一角色；把中文版缩到与英文同脸高后指标几乎不动（0.690→0.688、27.9→27.7），排除尺度伪影。**判断身份差异必须用等脸高对照，不得用单帧目视，也不得只报相似度不看景别。**

**"在中文里严格要求人物按参考图生成"补不回来**（7 个变体，其余条件与基线完全相同）：

| 变体 | 做法 | 年龄 | 判定 |
| :--- | :--- | :--- | :--- |
| C1 | 中文 +【身份第一原则】+ 五官比例锚点（眼距/鼻翼/三等分/下颌角逐项写死）+ 尾镜重复钉脸 | 28.7 | ❌ 完全无效，相似度反降（0.677→0.642） |
| C2 | C1 + 皮肤年轻化（删鱼尾纹/胡青/T 区油光，改"年轻干净的少年脸"） | 29.4 | ❌ 反而更老 |
| C3 | **英文** + 同样皮肤年轻化（对照） | 25.8 | 与英文基线持平 ⇒ **"皮肤描述催老"假设不成立**，该因子与语言无关 |
| C4 | 中文 + 量化景别（"头部约占画面高度三分之一" + "不要变成大特写"） | 29.4 | ⚠️ 景别 0.747，年龄无效 |
| C7 | 中文 + **纯正面**量化景别（去掉全部否定句与"特写"字样） | 27.1 | ⚠️ 景别 0.544，明显优于 C4 ⇒ **否定式反噬** |
| C6 | **混合**：中文正文，但 `<Subject N>` 段 + `retention_analysis` 该条 + `[Shot 3]` 段整段用英文 | 27.4 | ✅ 景别回到 0.415（英文层），年龄仍在中文层 |

由此得三条硬纪律（本库视频提示词仍**一律英文**，以下仅在确有必要写中文时适用）：

1. **措辞层面加不出身份**：加"身份第一原则""五官比例锚点""逐镜钉脸"无效，把催老描述删干净也无效。中文正文的年龄代价只能靠**语言层之外**的手段解决（加一张脸部大特写参考图、或后期人脸对齐），继续堆字没有意义。
2. **唯一有效杠杆是量化景别，且必须正面表述**：写"头部约占画面高度三分之一"这类数字约束有效；写"不要推近、不要变大特写"**会反噬**（C4 脸高 0.747 → 去掉否定句的 C7 降到 0.544）——与「否定式指令在 H3 上不可靠」是同一条规律，中文英文都成立。
3. **最该保留英文的是身份段**：中文正文 + `<Subject N>` 段 / `retention_analysis` 该条 / 尾镜段回英文（C6），是唯一能把景别拉回英文层的写法，代价是这三段读不了中文。

**统计口径（引用上述数字时必须带上）**：每个条件只有 1 次生成（4~5 帧取均值）。年龄分层零重叠、方向跨 seed 一致，可信度较高；相似度与景别的单组差异（0.02~0.05 / 0.05~0.4）与随机波动同量级，**不可当结论**。年龄与相似度用 buffalo_l（`det_10g` + `w600k_r50` + `genderage`），非官方评估口径，且参考图是 CG 渲染（年龄模型把参考读成 24 岁女性）——**只看相对差异，绝对数值不可采信**。"中文为什么显熟"的机理**未查明**（皮肤质感假设已被 C2/C3 否掉）。完整报告见 `docs\MiniMax-H3-中英提示词差异调研-2026-09-18.md`，数字原始数据见 `scripts\H3身份补足-指标.json`。

### 万物建模

即文生图并优化图片，包含ID，主-正词(TEXT)，主-负词(TEXT)，宽度(INT)，高度(INT)，图2(IMAGE)，图3(IMAGE)，优化-正词(TEXT)，优化-负词(TEXT)

#### 写入时机

万物只在正文中**第一次出现**时才建立建模条目（记录其首次外观与设定），后续不重复建。

#### 万物建模 提示词提取规范

**背景硬性要求：画面背景必须完全纯净的纯白色（#FFFFFF），无任何环境、地面、光影、纹理与装饰元素；主体独立悬浮在纯白背景上，不投射阴影、不带任何场景。**

**画面只画一个视角的完整外观**（单一完整画面）：不画三视图、不划分特写分区、不使用九宫格。取哪个视角按对象类型定——人物/生物取**正面全身**（从头到脚完整入画），物品取**最能看清本体特征的那一面**（以正面为主，形态特殊的可取斜面或俯视，但整幅仍只有一个画面）。

**宽度/高度按对象固有形态选**（见「生图类尺寸规范」）：

- **人物、生物、立式器物等站立/竖高对象** → 竖屏 9:16 = **768×1344**
- **横向铺展的物品** → 横屏 16:9 = **1344×768**
- **正方/圆形等无明显横竖倾向的对象** → 1:1 = **768×768**

提示词中必须逐条写出该画面要表现的外观特征（外形轮廓、结构组成、材质质感、颜色配色、纹样装饰、配件部件）；**严禁出现"三视图""全身三视图""物品三视图""正视/侧视/背视""左边特写"等字样**。

**按对象类型区分取景**：

- **人物**：**正面全身立绘**，人物居中构图，从头到脚完整入画，画面只呈现这一个正面形象（不画侧面与背面）；竖屏 768×1344
- **生物/怪物**：同人物，正面全身、完整入画，只呈现一个形象；竖屏 768×1344
- **物品**：**单一视角的完整外观**，不并排多个视角、不写"物品三视图"字样；横屏或竖屏按本体固有形态选，正方/圆形等用 768×768

### 灰度遮罩

即将原图转为灰度遮罩图，包含ID，原图(IMAGE)

#### 写入时机

**需要我明确说明写入哪些内容时，才可以写入**；分析正文内容时，**不允许自动写入**。

### 万物变化

即图片换样式并优化图片，包含ID，原图(IMAGE)，灰度遮罩(MASK)，图1(IMAGE)，图2(IMAGE)，图3(IMAGE)，变化-正词(TEXT)，变化-负词(TEXT)

#### 写入时机

仅当该万物在正文中**多次出现且外观/状态发生变化**时才写变化条目，并以**第一次出现的万物建模 ID** 为基础（变化条目的 原图/图1 引用该建模 ID）。

#### 万物变化 提示词提取规范

**背景硬性要求：变化后画面背景必须保持完全纯净的纯白色（#FFFFFF），无任何环境、地面、光影、纹理与装饰元素；主体独立悬浮在纯白背景上，不投射阴影、不带任何场景。**

变化以原图为底，必须**保留画面构图**：

1. **构图不变**：保持原图的单一完整画面构图——主体位置、大小、视角与四周留白全部不动，不新增显示区域、不换视角、不加特写
2. **主体外观修改**：按需求改变主体的材质、纹路、配色、氛围等外观表现，逐条描述改后的新外观
3. **整体统一**：主体各部分（各部件、各区域）修改后风格必须完全统一，不得出现相互矛盾或风格割裂

提示词中必须写明"保持原构图不变"；逐条描述主体改后的新外观并强调整体统一。**严禁出现"三视图""正视/侧视/背视""左边特写"等字样**。

#### Qwen-Image-Edit 提示写法（图片修改通用）

万物变化走 Qwen-Image-Edit 多图参考编辑，提示词写作要点：

1. **图号引用**：提示词里用 `图1` / `图2` / `图3`（中文工作流通行）或 `Picture 1/2/3`（正规标签）指代各张输入图，三图顺序对应 图1/图2/图3，接错顺序等于指错图
2. **surgical 指令**：指令只描述"改什么"（surgical instruction），别说长篇大论；文字编辑把目标文字用引号括起
3. **底图与参考**：以某张图为底、其余做参考时，目标图接在 图1（image1），参考图接 图2/图3（image2/image3）
4. **负提示词**：原生 Qwen-Image-Edit 负提示词支持有限，留空或单空格即可
5. **不要手写** `<|vision_start|><|image_pad|><|vision_end|>`：节点自动前缀，手写会破坏模板

### 场景图片生成 九宫格画面描述要求（场景首帧 / 场景拉镜 / 场景推镜 / 场景旋镜 通用）

场景类图片生成的提示词，必须把画面按**横向、纵向各三等分**划分为**九宫格**，并逐格点名每格内出现的元素与布局（场景方位、物件、灯光、细节），使画面各区域有明确交代、不随场。

描述顺序按画面宽高比分为 **16:9** 与 **9:16** 两种，按生图的宽度/高度选用对应顺序：

**16:9（横版）**：按 **左边 → 中间 → 右边** 描述。
- 左边：按 **左上 → 左边 → 左下** 描述；
- 中间：按 **上边 → 中间 → 下边** 描述；
- 右边：按 **右上 → 右边 → 右下** 描述。

**9:16（竖版）**：按 **上边 → 中间 → 下边** 描述。
- 上边：按 **上左 → 上边 → 上右** 描述；
- 中间：按 **左边 → 中间 → 右边** 描述；
- 下边：按 **下左 → 下边 → 下右** 描述。

每格用方位词开头点名（如"中间是…""左上…"），说明该处放的物件或留空；没有内容的格可略过，但任一元素只归入其所处的那一格，不得在不同格重复堆叠。

九宫格与场景本身的空间布局（前后左右、物件相互位置关系，见技能 `stories-scene-video` 的「场景类公共要求」）要互相印证统一：中间格通常为核心主体/主光区，四边交代墙面/地平/边缘，四角补足角部朝向的底景与照明。

### 场景首帧

即文生场景第一张图并优化图片，包含ID，主-正词(TEXT)，主-负词(TEXT)，宽度(INT)，高度(INT)，图2(IMAGE)，图3(IMAGE)，优化-正词(TEXT)，优化-负词(TEXT)

#### 写入时机

当一个场景在正文中**第一次出现**时，才写入 场景首帧 条目：为该场景定义**前面（正面）画面**的九宫格描述（场景大小、整体布局、各区域摆放逐格点名，见「场景图片生成 九宫格画面描述要求」）；该场景后续再次出现时不再重复建首帧。

### 场景拉镜

即镜头后移后的场景图片，也就是图片扩图并优化，包含ID，原场景(IMAGE)，扩展-正词(TEXT)，扩展-负词(TEXT)，图2(IMAGE)，图3(IMAGE)，优化-正词(TEXT)，优化-负词(TEXT)

#### 写入时机

**需要我明确说明写入哪些内容时，才可以写入**；分析正文内容时，**不允许自动写入**。

### 场景推镜

即镜头前推后的场景图片，也就是截取图片一块区域显示细节并优化，包含ID，原场景(IMAGE)，图2(IMAGE)，图3(IMAGE)，主-正词(TEXT)，主-负词(TEXT)。注意：推镜为「先截取放大 → 再 qwen-edit 精修」两段式，编辑底图（图1）是工作流内放大后的特写结果，由内部衔接，不在表中填写图1

#### 写入时机

**需要我明确说明写入哪些内容时，才可以写入**；分析正文内容时，**不允许自动写入**。

### 场景旋镜

即利用视角镜头Lora实现对场景不同视角生成图片，包含ID，原视角(IMAGE)，前右-正词(TEXT)，右面-正词(TEXT)，右后-正词(TEXT)，后面-正词(TEXT)，后左-正词(TEXT)，左面-正词(TEXT)，左前-正词(TEXT)，全图-正词(TEXT)，前右-负词(TEXT)，右面-负词(TEXT)，右后-负词(TEXT)，后面-负词(TEXT)，后左-负词(TEXT)，左面-负词(TEXT)，左前-负词(TEXT)，全图-负词(TEXT)。原视角即正面参考图，工作流中固定连入 image1 参考位。负词按面各一列，工作流中「视角提示词 切换」与「负面提示词 切换」两个下拉框按同一顺序（前右、右面、右后、后面、后左、左面、左前、全图八项）填充并共享同一选择，选某方位时同步切换。工作流中负词与正词同用带图片编码节点(TextEncodeQwenImageEditPlus)，均携带原视角(正面参考图)参考图。全图为旋镜序列第九张图（正上方垂直俯瞰），与原视角及七面共九图，拼 3×3 九宫格参考图时八面绕周圈顺时针排列、全图居中；八方位（含全图）均进入「视角提示词 切换」「负面提示词 切换」下拉（全图居第 8 位），选全图同样携带 原视角（正面参考图）参考图生成，其俯视机位按「全图方位」要求另行描述

#### 写入时机

**需要我明确说明写入哪些内容时，才可以写入**；分析正文内容时，**不允许自动写入**。

#### 场景旋镜 提示词提取规范

八视角按「前→前右→右→右后→后→后左→左→左前」顺时针排列：

1. **原视角默认为前视图**，直接引用已有场景图作为正面参考图（工作流中固定连入 image1 参考位）
2. **提示词固定前缀写法**（x面 ∈ 前右/右面/右后/后面/后左/左面/左前；前缀与画面内容描述用 `<br>` 分割；内容描述以「x面是」点面开头，按九宫格顺序逐段写，以 `整体是…` 收尾）：
   - 前右-正词：`图1 是正面参考图<br>当前在生成前右，画面描述如下：<br>` + 前右画面内容描述
   - 右面-正词：`图1 是正面参考图<br>当前在生成右面，画面描述如下：<br>` + 右面画面内容描述
   - 右后-正词：`图1 是正面参考图<br>当前在生成右后，画面描述如下：<br>` + 右后画面内容描述
   - 后面-正词：`图1 是正面参考图<br>当前在生成后面，画面描述如下：<br>` + 后面画面内容描述
   - 后左-正词：`图1 是正面参考图<br>当前在生成后左，画面描述如下：<br>` + 后左画面内容描述
   - 左面-正词：`图1 是正面参考图<br>当前在生成左面，画面描述如下：<br>` + 左面画面内容描述
   - 左前-正词：`图1 是正面参考图<br>当前在生成左前，画面描述如下：<br>` + 左前画面内容描述
3. **全图**俯瞰画面（第九张图）不适用上述 x面 固定前缀模板，写入 全图-正词 列，按「全图方位」要求另写俯视平面图描述（逐区域点名）；其负词写入 全图-负词 列

## 表格 md 文件规范说明

所有表格 md 文件的第一列永远是 ID

后续字段完全按照 标题(类型) 定义

(类型) 不填写默认是 (STRING)

(类型) 包含如下内容和说明:

- (IMAGE)/(VIDEO)/(AUDIO)/(MASK)/(STRING)/(INT)/(FLOAT)/(BOOLEAN)：对应 ComfyUI 同名输出类型
- (TEXT)：对应 ComfyUI 的 STRING 输出类型，但自定义节点渲染为多行文本框

实际使用情况（扫描 `stories/` 下全部含表格 md 文件）：显式声明过的只有 `TEXT`、`IMAGE`、`INT`、`VIDEO`、`AUDIO`、`FLOAT`、`MASK` 七种；`STRING` 从未显式书写（各资源表的 ID 列等一律靠省略默认）；`BOOLEAN` 已登记但尚无一处使用。

各类型的字段定义（ID + 各字段(类型)）见对应类型的技能（`stories-resource-tables` 的图片类、`stories-scene-video` 的场景类、`stories-video-prompts` 的视频类、`stories-audio-resources` 的音频类）；ID 命名规则见 `stories/AGENTS.md` 的「资源 ID 规则」。

### 全图方位

**全图**是第九个方位，代表从场景**正上方垂直向下俯瞰**的全景画面（顶视/Bird's Eye），是八面水平方位之外的垂直维度补充。

- **含义**：全图不是某一朝向的画面，而是俯瞰整个场景的鸟瞰平面图——能看到场景中所有物件、地面、边界与空间关系的完整布局
- **用途**：场景类资源描述场景空间时，除八面水平方位外，还应提供全图方位的整体俯瞰描述，使模型从二维平面理解整个场景的空间结构与物件分布
- **描述要求**：全图方位的描述按俯视平面图的方位词逐区域点名（如「上方是…」「下方是…」「左侧是…」「右侧是…」「中央是…」），与八面水平方位描述交叉印证
- **与其他方位的关系**：全图方位是八面水平方位的上层概览——八面描述的是"站在某个方向往另一个方向看"，全图描述的是"从天上看下来"；两者互补而非替代
